import React from "react";
import { Text, StyleSheet, View } from "react-native";

const ImageDetail = () => {
    return <Text>Detalle de Imagen</Text>
};

const styles = StyleSheet.create({

});

export default ImageDetail;